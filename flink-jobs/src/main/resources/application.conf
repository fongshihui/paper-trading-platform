# Placeholder application configuration for the SignalJob

kafka {
  bootstrap.servers = "kafka:9092"
  topics {
    market-data = "market-data"
    signals = "signals"
  }
}

job {
  name = "PaperTrading-SignalJob"
  momentum-threshold = 0.001  # 10 bps
  quantity = 10
}

# Note: the Java job currently reads configuration from environment
# variables (KAFKA_BOOTSTRAP_SERVERS, MARKET_DATA_TOPIC, SIGNALS_TOPIC).
# This file is provided as an example for future extension.
